{% extends "base.html.twig" %}

{% block body %}
	<div class="container">
		<h1>{{ person.lastname }}
			{{ person.firstname }}</h1>
		<table class="table table-striped">
			<tr>
				<td>Sites :</td>
				<td>
				{% for localisation in person.localisations %}
					{{ localisation.name }} , 
				{% endfor %}
				</td>
			</tr>
		</table>
	</div>
{% endblock %}
